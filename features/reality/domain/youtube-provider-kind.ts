/** Mismo patrón que `email-provider-kind.ts`: re-export de `core/youtube-connections/domain` (core/ no puede importar de features/). */
export type { YoutubeProviderKind } from "../../../core/youtube-connections/domain";
export { YOUTUBE_PROVIDER_KINDS } from "../../../core/youtube-connections/domain";
